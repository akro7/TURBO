<Application x:Class="MKVenomTool.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="ekoflash.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="MainBgBrush" Color="#050A14"/>
            <SolidColorBrush x:Key="PanelBrush" Color="#CC0D1B33"/>
            <SolidColorBrush x:Key="BorderBrush" Color="#3352BFFF"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#00E5FF"/>
            <SolidColorBrush x:Key="AccentSoftBrush" Color="#1A00E5FF"/>
            <SolidColorBrush x:Key="DangerBrush" Color="#FF4C78"/>
            <SolidColorBrush x:Key="SuccessBrush" Color="#1CE17A"/>
            <SolidColorBrush x:Key="TextMutedBrush" Color="#88B4D8"/>

            <SolidColorBrush x:Key="BgRoot" Color="#05070D"/>
            <SolidColorBrush x:Key="BgPanel" Color="#0D1626"/>
            <SolidColorBrush x:Key="BgCard" Color="#101B2F"/>
            <SolidColorBrush x:Key="BgInput" Color="#0F1A2B"/>
            <SolidColorBrush x:Key="BgHover" Color="#1F2D47"/>
            <SolidColorBrush x:Key="FgBright" Color="#EAF4FF"/>
            <SolidColorBrush x:Key="FgMid" Color="#88B4D8"/>
            <SolidColorBrush x:Key="FgDim" Color="#2A3F5F"/>

            <Style TargetType="Window">
                <Setter Property="Background" Value="{DynamicResource MainBgBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource FgBright}"/>
                <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource FgBright}"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{DynamicResource BgInput}"/>
                <Setter Property="Foreground" Value="{DynamicResource FgBright}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,5"/>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{DynamicResource BgInput}"/>
                <Setter Property="Foreground" Value="{DynamicResource FgBright}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="6,4"/>
            </Style>

            <Style TargetType="Button">
                <Setter Property="Foreground" Value="{DynamicResource FgBright}"/>
                <Setter Property="Background" Value="#18243B"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>

            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                <Setter Property="Foreground" Value="#050A14"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>

            <Style x:Key="ModeButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Height" Value="38"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>

            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="{DynamicResource BgCard}"/>
                <Setter Property="Foreground" Value="{DynamicResource FgBright}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="RowBackground" Value="{DynamicResource BgCard}"/>
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource BgPanel}"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource BgPanel}"/>
                <Setter Property="Foreground" Value="{DynamicResource FgMid}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
