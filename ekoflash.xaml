<Window x:Class="MKVenomTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TURBO FLASH TOOL - v1.0" 
        Width="1340" Height="860"
        MinWidth="1150" MinHeight="750"
        WindowStartupLocation="CenterScreen"
        Background="#050A14" Foreground="#EAF4FF"
        FontFamily="Segoe UI Semibold">

    <Window.Resources>
        <SolidColorBrush x:Key="MainBgBrush"     Color="#050A14"/>
        <SolidColorBrush x:Key="PanelBrush"      Color="#CC0D1B33"/>
        <SolidColorBrush x:Key="BorderBrush"     Color="#3352BFFF"/>
        <SolidColorBrush x:Key="AccentBrush"     Color="#00E5FF"/>
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#1A00E5FF"/>
        <SolidColorBrush x:Key="DangerBrush"     Color="#FF4C78"/>
        <SolidColorBrush x:Key="SuccessBrush"    Color="#1CE17A"/>
        <SolidColorBrush x:Key="TextMutedBrush"  Color="#88B4D8"/>

        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background"      Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="CornerRadius"    Value="15"/>
            <Setter Property="Padding"         Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Color="#000000" Opacity="0.4" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ✅ PathBox Style مضاف -->
        <Style x:Key="PathBox" TargetType="TextBox">
            <Setter Property="Background"       Value="#0F1A2B"/>
            <Setter Property="Foreground"       Value="#B2DFFF"/>
            <Setter Property="BorderBrush"      Value="#2A3F5F"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="8,5"/>
            <Setter Property="FontFamily"       Value="Consolas"/>
            <Setter Property="FontSize"         Value="11"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModeButton" TargetType="Button">
            <Setter Property="Foreground"      Value="#D6E9FF"/>
            <Setter Property="Background"      Value="#152033"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height"          Value="40"/>
            <Setter Property="Margin"          Value="0,0,10,0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1F2D47"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FlashBtn" TargetType="Button">
            <Setter Property="Background"      Value="#00E5FF"/>
            <Setter Property="Foreground"      Value="#050A14"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="WindowOnLoad">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,40,0,-40" To="0" Duration="0:0:0.6">
                <ThicknessAnimation.EasingFunction>
                    <PowerEase Power="5" EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowOnLoad}"/>
        </EventTrigger>
    </Window.Triggers>

    <Grid Background="{StaticResource MainBgBrush}">
        <Canvas IsHitTestVisible="False">
            <Ellipse Canvas.Left="-150" Canvas.Top="-150" Width="500" Height="500" Fill="#1500E5FF" Opacity="0.3">
                <Ellipse.Effect><BlurEffect Radius="120"/></Ellipse.Effect>
            </Ellipse>
            <Ellipse Canvas.Right="-100" Canvas.Bottom="-100" Width="450" Height="450" Fill="#155500FF" Opacity="0.2">
                <Ellipse.Effect><BlurEffect Radius="150"/></Ellipse.Effect>
            </Ellipse>
        </Canvas>

        <Border Margin="15" Style="{StaticResource GlassPanel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="180"/>
                </Grid.RowDefinitions>

                <!-- ══ ROW 0: Header ══ -->
                <Grid Grid.Row="0" Margin="5,0,5,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Width="70" Height="70" CornerRadius="15" Background="#10182D"
                            BorderBrush="{StaticResource AccentBrush}" BorderThickness="2" Margin="0,0,15,0">
                        <Grid>
                            <TextBlock Text="T" FontSize="36" FontWeight="Black"
                                       Foreground="{StaticResource AccentBrush}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Ellipse Width="50" Height="50" Stroke="{StaticResource AccentBrush}"
                                     StrokeThickness="1" Opacity="0.3" StrokeDashArray="4 2"/>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="TURBO FLASH TOOL" FontSize="42" FontWeight="Black" Foreground="White">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="10" Color="#00E5FF" Opacity="0.5" ShadowDepth="0"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Developers: " Foreground="{StaticResource TextMutedBrush}" FontSize="13"/>
                            <TextBlock Text="AHMED YOUNIS &amp; Mohamed Khaled" Foreground="{StaticResource AccentBrush}" FontSize="13" FontWeight="Bold"/>
                            <TextBlock Text=" | System v2.6.0" Foreground="#5A8BAA" FontSize="13" Margin="10,0,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                        <TextBlock Text="Custom Themes" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,5,10,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="25" Height="25" Background="#00E5FF" Click="Swatch_Click" Tag="Blue"/>
                        <Button Style="{StaticResource ModeButton}" Width="25" Height="25" Background="#A100FF" Click="Swatch_Click" Tag="Purple" Margin="5,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="25" Height="25" Background="#FF0055" Click="Swatch_Click" Tag="Crimson"/>
                    </StackPanel>
                </Grid>

                <!-- ══ ROW 1: Mode Buttons + Device Status ══ -->
                <Grid Grid.Row="1" Margin="5,0,5,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- ✅ x:Name مضاف للأزرار الأربعة -->
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="FastbootBtn" Content="FASTBOOT ENGINE" Style="{StaticResource ModeButton}" Width="160" Click="FastbootMode_Click"/>
                        <Button x:Name="OdinBtn"     Content="SAMSUNG ODIN"    Style="{StaticResource ModeButton}" Width="160" Click="OdinMode_Click"/>
                        <Button x:Name="SideloadBtn" Content="ADB SIDELOAD"    Style="{StaticResource ModeButton}" Width="160" Click="SideloadMode_Click"/>
                        <Button x:Name="ToolsBtn"    Content="SYSTEM TOOLS"    Style="{StaticResource ModeButton}" Width="160" Click="ToolsMode_Click"/>
                    </StackPanel>

                    <Border Grid.Column="1" Background="#101B2F" CornerRadius="10" Padding="15,5"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel VerticalAlignment="Center" Margin="0,0,15,0">
                                <TextBlock Text="DEVICE CONNECTIVITY" Foreground="{StaticResource TextMutedBrush}" FontSize="10" FontWeight="Bold"/>
                                <TextBlock x:Name="DeviceStatusText" Text="DISCONNECTED" Foreground="{StaticResource DangerBrush}" FontSize="12" FontWeight="Black"/>
                            </StackPanel>
                            <Button Content="SCAN DEVICE" Style="{StaticResource FlashBtn}" Width="110" Height="32" Click="DetectDevice_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- ══ ROW 2: Main Content ══ -->
                <Grid Grid.Row="2" Margin="5,0,5,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <!-- Main Panel Area -->
                    <Border Style="{StaticResource GlassPanel}" Background="#0D1626">
                        <Grid>
                            <!-- ✅ PanelFastboot -->
                            <Grid x:Name="PanelFastboot" Visibility="Visible">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="FLASH PARTITION MANAGER" FontSize="18"
                                           Foreground="{StaticResource AccentBrush}" Margin="0,0,0,15" FontWeight="Bold"/>
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="RowsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="90"/>
                                                        <ColumnDefinition Width="80"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Label}" VerticalAlignment="Center" Foreground="#B2DFFF" FontSize="12"/>
                                                    <TextBox Grid.Column="1" Style="{StaticResource PathBox}" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"/>
                                                    <Button Grid.Column="2" Content="BROWSE" Style="{StaticResource ModeButton}" Margin="5,0" Click="Browse_Click" Tag="{Binding Key}"/>
                                                    <Button Grid.Column="3" Content="FLASH"  Style="{StaticResource FlashBtn}" Click="FlashOne_Click" Tag="{Binding Key}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>

                            <!-- ✅ PanelOdin -->
                            <Grid x:Name="PanelOdin" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="SAMSUNG ODIN FLASHER" FontSize="18"
                                           Foreground="#FFB830" Margin="0,0,0,15" FontWeight="Bold"/>
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="OdinRowsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="90"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Label}" VerticalAlignment="Center" Foreground="#FFD580" FontSize="12"/>
                                                    <TextBox Grid.Column="1" Style="{StaticResource PathBox}" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"/>
                                                    <Button Grid.Column="2" Content="BROWSE" Style="{StaticResource ModeButton}" Margin="5,0" Click="Browse_Click" Tag="{Binding Key}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>

                            <!-- ✅ PanelSideload -->
                            <Grid x:Name="PanelSideload" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="ADB SIDELOAD" FontSize="18"
                                           Foreground="{StaticResource SuccessBrush}" Margin="0,0,0,15" FontWeight="Bold"/>
                                <StackPanel Grid.Row="1" VerticalAlignment="Top">
                                    <TextBlock Text="Select ZIP file to sideload:" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="SideloadPathBox" Style="{StaticResource PathBox}"/>
                                        <Button Grid.Column="1" Content="BROWSE" Style="{StaticResource ModeButton}" Margin="5,0,0,0" Click="Browse_Click" Tag="sideload"/>
                                    </Grid>
                                    <Button Content="START SIDELOAD" Style="{StaticResource FlashBtn}" Height="40" Margin="0,15,0,0" Click="FlashAll_Click"/>
                                </StackPanel>
                            </Grid>

                            <!-- ✅ PanelTools -->
                            <Grid x:Name="PanelTools" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="SYSTEM TOOLS" FontSize="18"
                                           Foreground="{StaticResource DangerBrush}" Margin="0,0,0,15" FontWeight="Bold"/>
                                <StackPanel Grid.Row="1">
                                    <Button Content="INSTALL ADB DRIVERS (ZADIG)" Style="{StaticResource ModeButton}" Height="40" Margin="0,0,0,8" Width="Auto" Click="QuickCmd_Click" Tag="zadig"/>
                                    <Button Content="REBOOT TO RECOVERY" Style="{StaticResource ModeButton}" Height="40" Margin="0,0,0,8" Width="Auto" Click="QuickCmd_Click" Tag="adb reboot recovery"/>
                                    <Button Content="REBOOT TO DOWNLOAD MODE" Style="{StaticResource ModeButton}" Height="40" Margin="0,0,0,8" Width="Auto" Click="QuickCmd_Click" Tag="adb reboot download"/>
                                    <Button Content="WIPE CACHE PARTITION" Style="{StaticResource ModeButton}" Height="40" Width="Auto" Foreground="{StaticResource DangerBrush}" Click="QuickCmd_Click" Tag="fastboot erase cache"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Right Panel -->
                    <StackPanel Grid.Column="1" Margin="15,0,0,0">

                        <!-- ✅ Tab Buttons -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Button x:Name="TabCmdBtn" Content="COMMANDS" Style="{StaticResource ModeButton}"
                                    Width="140" Click="TabCmd_Click"/>
                            <Button x:Name="TabOptBtn" Content="OPTIONS" Style="{StaticResource ModeButton}"
                                    Width="140" Click="TabOptions_Click"/>
                        </StackPanel>

                        <!-- ✅ TabCmdPanel -->
                        <StackPanel x:Name="TabCmdPanel">
                            <Border Style="{StaticResource GlassPanel}" Height="200" Margin="0,0,0,12">
                                <Grid>
                                    <TextBlock Text="COMMAND PREVIEW" FontSize="12" Foreground="{StaticResource AccentBrush}"
                                               FontWeight="Bold" Margin="0,0,0,5" VerticalAlignment="Top"/>
                                    <TextBox x:Name="CommandPreviewBox" Margin="0,25,0,0" IsReadOnly="True"
                                             Background="#08101E" BorderThickness="0" Foreground="#00FF95"
                                             FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                                </Grid>
                            </Border>

                            <Border Style="{StaticResource GlassPanel}">
                                <StackPanel>
                                    <TextBlock Text="QUICK ACTIONS" FontSize="12" Foreground="{StaticResource AccentBrush}"
                                               FontWeight="Bold" Margin="0,0,0,10"/>
                                    <Button Content="REBOOT TO SYSTEM"    Style="{StaticResource ModeButton}" Width="Auto" Margin="0,0,0,8" Tag="adb reboot"            Click="QuickCmd_Click"/>
                                    <Button Content="REBOOT BOOTLOADER"   Style="{StaticResource ModeButton}" Width="Auto" Margin="0,0,0,8" Tag="adb reboot bootloader" Click="QuickCmd_Click"/>
                                    <Button Content="ERASE USERDATA" Style="{StaticResource ModeButton}" Width="Auto" Foreground="{StaticResource DangerBrush}" Tag="fastboot erase userdata" Click="QuickCmd_Click"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- ✅ TabOptionsPanel -->
                        <StackPanel x:Name="TabOptionsPanel" Visibility="Collapsed">
                            <Border Style="{StaticResource GlassPanel}">
                                <StackPanel>
                                    <TextBlock Text="FLASH OPTIONS" FontSize="12" Foreground="{StaticResource AccentBrush}"
                                               FontWeight="Bold" Margin="0,0,0,10"/>
                                    <CheckBox Content="Skip verification" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,8"/>
                                    <CheckBox Content="Wipe userdata after flash" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,8"/>
                                    <CheckBox Content="Reboot after flash" Foreground="{StaticResource TextMutedBrush}" IsChecked="True"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                    </StackPanel>
                </Grid>

                <!-- ══ ROW 3: Progress ══ -->
                <Grid Grid.Row="3" Margin="5,15,5,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock x:Name="ProgressStatusText" Text="Ready to Process..."
                                   Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,0,0,5"/>
                        <ProgressBar x:Name="MainProgress" Height="12" Background="#101B2F"
                                     Foreground="{StaticResource AccentBrush}" BorderThickness="0">
                            <ProgressBar.Effect>
                                <BlurEffect Radius="2" RenderingBias="Quality"/>
                            </ProgressBar.Effect>
                        </ProgressBar>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Bottom">
                        <Button x:Name="FlashAllBtn" Content="START FLASHING" Style="{StaticResource FlashBtn}"
                                Width="180" Height="40" FontSize="14" Click="FlashAll_Click"/>
                        <Button x:Name="CancelBtn"   Content="STOP"           Style="{StaticResource ModeButton}"
                                Width="90"  Height="40" Margin="10,0,0,0" Foreground="{StaticResource DangerBrush}" Click="Cancel_Click"/>
                    </StackPanel>
                </Grid>

                <!-- ══ ROW 4: Log Terminal ══ -->
                <Border Grid.Row="4" Margin="5,0,5,5" Background="#03070F" CornerRadius="10"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <Grid>
                        <TextBlock Text="TURBO TERMINAL OUTPUT" Foreground="#3352BFFF" FontSize="10"
                                   FontWeight="Bold" Margin="10,5" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                        <TextBox x:Name="LogBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                                 Background="Transparent" Foreground="#D7F1FF" BorderThickness="0"
                                 FontFamily="Consolas" FontSize="12" Padding="12" AcceptsReturn="True"/>
                    </Grid>
                </Border>

            </Grid>
        </Border>
    </Grid>
</Window>

<!-- =====================================================
     TURBO FLASH TOOL — Backup & Restore Panel
     Developers: AHMED YOUNIS & Mohamed Khaled
     Add this inside MainWindow's main Grid, alongside
     PanelFastboot, PanelOdin, PanelSideload, PanelTools
     ===================================================== -->

<!-- Step 1: Add Button in the mode bar (alongside existing buttons) -->
<Button x:Name="BackupBtn"
        Content="BACKUP &amp; RESTORE"
        Style="{StaticResource ModeButtonStyle}"
        Click="BackupMode_Click"
        Margin="4,0,0,0"/>

<!-- =====================================================
     Step 2: Main Backup & Restore Panel
     Place inside the same container as PanelFastboot etc.
     ===================================================== -->
<Grid x:Name="PanelBackup" Visibility="Collapsed">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Panel Header -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
        <TextBlock Text="💾  BACKUP &amp; RESTORE"
                   FontSize="15" FontWeight="Black"
                   Foreground="{DynamicResource AccentBrush}"
                   VerticalAlignment="Center"
                   LetterSpacing="2"/>
        <Border Margin="12,0,0,0"
                Background="{DynamicResource AccentSoftBrush}"
                BorderBrush="{DynamicResource AccentBrush}"
                BorderThickness="1" CornerRadius="4"
                Padding="6,2">
            <TextBlock Text="ROOT REQUIRED"
                       FontSize="10" FontWeight="Bold"
                       Foreground="{DynamicResource AccentBrush}"
                       LetterSpacing="1"/>
        </Border>
    </StackPanel>

    <!-- Sub Tab Buttons: Backup / Restore -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,14">
        <Button x:Name="BackupSubBtn"
                Content="📤  BACKUP"
                Tag="backup"
                Click="BackupSubTab_Click"
                Style="{StaticResource SubTabButtonStyle}"/>
        <Button x:Name="RestoreSubBtn"
                Content="📥  RESTORE"
                Tag="restore"
                Click="BackupSubTab_Click"
                Margin="6,0,0,0"
                Style="{StaticResource SubTabButtonStyle}"/>
    </StackPanel>

    <!-- ============ BACKUP SUB-PANEL ============ -->
    <Grid x:Name="BackupSubPanel" Grid.Row="2" Visibility="Visible">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- Left: App List + Options -->
        <Grid Grid.Column="0" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Backup Options -->
            <Border Grid.Row="0"
                    Background="rgba(255,255,255,0.02)"
                    BorderBrush="rgba(255,255,255,0.07)"
                    BorderThickness="1" CornerRadius="8"
                    Padding="12,10" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="BACKUP OPTIONS"
                               FontSize="11" FontWeight="Bold"
                               Foreground="#7899BB"
                               LetterSpacing="1"
                               Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <CheckBox x:Name="ChkBackupApk"
                                  Content="APK"
                                  IsChecked="True"
                                  Foreground="{DynamicResource AccentBrush}"
                                  Style="{StaticResource TurboCheckBoxStyle}"/>
                        <CheckBox x:Name="ChkBackupData"
                                  Content="Data"
                                  IsChecked="True"
                                  Foreground="{DynamicResource AccentBrush}"
                                  Style="{StaticResource TurboCheckBoxStyle}"/>
                        <CheckBox x:Name="ChkBackupUserDe"
                                  Content="User_DE"
                                  IsChecked="False"
                                  Foreground="{DynamicResource AccentBrush}"
                                  Style="{StaticResource TurboCheckBoxStyle}"/>
                        <CheckBox x:Name="ChkBackupObb"
                                  Content="OBB"
                                  IsChecked="False"
                                  Foreground="{DynamicResource AccentBrush}"
                                  Style="{StaticResource TurboCheckBoxStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- App List Header -->
            <Grid Grid.Row="1" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           x:Name="AppListHeader"
                           Text="INSTALLED APPS"
                           FontSize="11" Foreground="#7899BB"
                           LetterSpacing="1"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                    <Button Content="Select All"
                            FontSize="11"
                            Click="SelectAllApps_Click"
                            Style="{StaticResource SmallFlatButtonStyle}"/>
                    <Button Content="Clear"
                            FontSize="11"
                            Click="ClearApps_Click"
                            Style="{StaticResource SmallFlatButtonStyle}"/>
                    <Button Content="🔄 Refresh"
                            FontSize="11"
                            Click="RefreshApps_Click"
                            Style="{StaticResource SmallFlatButtonStyle}"/>
                </StackPanel>
            </Grid>

            <!-- App List Box -->
            <ListBox x:Name="AppListBox"
                     Grid.Row="2"
                     SelectionMode="Multiple"
                     Background="rgba(0,0,0,0.25)"
                     BorderBrush="rgba(255,255,255,0.07)"
                     BorderThickness="1"
                     MaxHeight="220"
                     Padding="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="rgba(255,255,255,0.04)"
                                            BorderThickness="0,0,0,1"
                                            Padding="{TemplateBinding Padding}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0"
                                                      IsChecked="{Binding IsSelected,
                                                        RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                      Margin="0,0,10,0"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontSize="13"
                                                           Foreground="#CCDDE0"/>
                                                <TextBlock Text="{Binding PackageName}"
                                                           FontSize="10"
                                                           Foreground="#445566"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background"
                                                    Value="{DynamicResource AccentSoftBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- Progress Bar -->
            <ProgressBar x:Name="BackupProgress"
                         Grid.Row="3"
                         Height="6"
                         Value="0"
                         Maximum="100"
                         Margin="0,12,0,4"
                         Foreground="{DynamicResource AccentBrush}"
                         Background="rgba(255,255,255,0.08)"/>

            <TextBlock x:Name="BackupProgressText"
                       Grid.Row="3"
                       Text=""
                       FontSize="11"
                       Foreground="{DynamicResource AccentBrush}"
                       Margin="0,20,0,0"
                       Visibility="Collapsed"/>

            <!-- Action Buttons -->
            <Grid Grid.Row="4" Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="StartBackupBtn"
                        Grid.Column="0"
                        Content="START BACKUP"
                        Click="StartBackup_Click"
                        Style="{StaticResource AccentButtonStyle}"
                        Margin="0,0,10,0"/>
                <Button x:Name="StopBackupBtn"
                        Grid.Column="1"
                        Content="STOP"
                        Click="StopBackup_Click"
                        Width="80"
                        Style="{StaticResource FlatButtonStyle}"/>
            </Grid>
        </Grid>

        <!-- Right: Log Panel (shared with main LogBox) -->
        <!-- The right side reuses the existing LogBox from MainWindow -->
        <!-- No extra XAML needed here — log output goes to LogBox -->

    </Grid>

    <!-- ============ RESTORE SUB-PANEL ============ -->
    <Grid x:Name="RestoreSubPanel" Grid.Row="2" Visibility="Collapsed">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="AVAILABLE BACKUPS"
                   FontSize="11" FontWeight="Bold"
                   Foreground="#7899BB"
                   LetterSpacing="1"
                   Margin="0,0,0,8"/>

        <!-- Backup Entries DataGrid -->
        <DataGrid x:Name="BackupListGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  MaxHeight="280"
                  Background="rgba(0,0,0,0.25)"
                  BorderBrush="rgba(255,255,255,0.07)"
                  BorderThickness="1"
                  RowBackground="Transparent"
                  AlternatingRowBackground="rgba(255,255,255,0.015)"
                  HorizontalGridLinesBrush="rgba(255,255,255,0.04)">
            <DataGrid.Columns>
                <DataGridTextColumn Header="APP NAME"
                                    Binding="{Binding DisplayName}"
                                    Width="*"/>
                <DataGridTextColumn Header="PACKAGE"
                                    Binding="{Binding PackageName}"
                                    Width="*"/>
                <DataGridTextColumn Header="DATE"
                                    Binding="{Binding BackupDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}"
                                    Width="140"/>
                <DataGridTemplateColumn Header="ACTION" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="RESTORE"
                                    Tag="{Binding BackupPath}"
                                    Click="RestoreEntry_Click"
                                    Style="{StaticResource AccentOutlineButtonStyle}"
                                    FontSize="11"
                                    Padding="8,4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Backup Root Path Info -->
        <TextBlock Grid.Row="2"
                   x:Name="BackupRootText"
                   FontSize="11"
                   Foreground="#445566"
                   Margin="0,10,0,0"/>
    </Grid>
</Grid>
