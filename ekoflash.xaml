<Window x:Class="MKVenomTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TURBO FLASH TOOL - v1.0"
        Width="1340" Height="860"
        MinWidth="1150" MinHeight="750"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MainBgBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        FontFamily="Segoe UI Semibold">

    <Window.Resources>
        <SolidColorBrush x:Key="MainBgBrush"       Color="#050A14"/>
        <SolidColorBrush x:Key="PanelBrush"        Color="#CC0D1B33"/>
        <SolidColorBrush x:Key="CardBrush"         Color="#101B2F"/>
        <SolidColorBrush x:Key="BgInputBrush"      Color="#0F1A2B"/>
        <SolidColorBrush x:Key="LogBgBrush"        Color="#03070F"/>
        <SolidColorBrush x:Key="CmdPreviewBgBrush" Color="#08101E"/>
        <SolidColorBrush x:Key="CmdPreviewFgBrush" Color="#00FF95"/>

        <SolidColorBrush x:Key="BorderBrush"       Color="#3352BFFF"/>
        <SolidColorBrush x:Key="AccentBrush"       Color="#00E5FF"/>
        <SolidColorBrush x:Key="AccentSoftBrush"   Color="#1A00E5FF"/>

        <SolidColorBrush x:Key="ForegroundBrush"   Color="#EAF4FF"/>
        <SolidColorBrush x:Key="TextMutedBrush"    Color="#88B4D8"/>

        <SolidColorBrush x:Key="ButtonBgBrush"     Color="#152033"/>
        <SolidColorBrush x:Key="ButtonHoverBrush"  Color="#1F2D47"/>
        <SolidColorBrush x:Key="FlashBtnFgBrush"   Color="#050A14"/>

        <SolidColorBrush x:Key="DangerBrush"       Color="#FF4C78"/>
        <SolidColorBrush x:Key="SuccessBrush"      Color="#1CE17A"/>

        <Style x:Key="GlassPanel" TargetType="Border">
            <Setter Property="Background"      Value="{DynamicResource PanelBrush}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="CornerRadius"    Value="15"/>
            <Setter Property="Padding"         Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" Color="#000000" Opacity="0.4" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PathBox" TargetType="TextBox">
            <Setter Property="Background"       Value="{DynamicResource BgInputBrush}"/>
            <Setter Property="Foreground"       Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush"      Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="8,5"/>
            <Setter Property="FontFamily"       Value="Consolas"/>
            <Setter Property="FontSize"         Value="11"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModeButton" TargetType="Button">
            <Setter Property="Foreground"      Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Background"      Value="{DynamicResource ButtonBgBrush}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height"          Value="40"/>
            <Setter Property="Margin"          Value="0,0,10,0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FlashBtn" TargetType="Button">
            <Setter Property="Background"      Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground"      Value="{DynamicResource FlashBtnFgBrush}"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Storyboard x:Key="WindowOnLoad">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.8"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowOnLoad}"/>
        </EventTrigger>
    </Window.Triggers>

    <Grid Background="{DynamicResource MainBgBrush}">
        <Canvas IsHitTestVisible="False">
            <Ellipse Canvas.Left="-150" Canvas.Top="-150" Width="500" Height="500" Fill="#1500E5FF" Opacity="0.3">
                <Ellipse.Effect><BlurEffect Radius="120"/></Ellipse.Effect>
            </Ellipse>
            <Ellipse Canvas.Right="-100" Canvas.Bottom="-100" Width="450" Height="450" Fill="#155500FF" Opacity="0.2">
                <Ellipse.Effect><BlurEffect Radius="150"/></Ellipse.Effect>
            </Ellipse>
        </Canvas>

        <Border Margin="15" Style="{StaticResource GlassPanel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="180"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="5,0,5,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Width="70" Height="70" CornerRadius="15"
                            Background="{DynamicResource CardBrush}"
                            BorderBrush="{DynamicResource AccentBrush}" BorderThickness="2" Margin="0,0,15,0">
                        <Grid>
                            <TextBlock Text="T" FontSize="36" FontWeight="Black"
                                       Foreground="{DynamicResource AccentBrush}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Ellipse Width="50" Height="50" Stroke="{DynamicResource AccentBrush}"
                                     StrokeThickness="1" Opacity="0.3" StrokeDashArray="4 2"/>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="TURBO FLASH TOOL" FontSize="42" FontWeight="Black" Foreground="{DynamicResource ForegroundBrush}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Developers: " Foreground="{DynamicResource TextMutedBrush}" FontSize="13"/>
                            <TextBlock Text="AHMED YOUNIS &amp; Mohamed Khaled" Foreground="{DynamicResource AccentBrush}" FontSize="13" FontWeight="Bold"/>
                            <TextBlock Text=" | System v2.6.0" Foreground="{DynamicResource TextMutedBrush}" FontSize="13" Margin="10,0,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                        <TextBlock Text="Custom Themes" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,5,10,0"/>

                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#00E5FF" Click="Swatch_Click" Tag="Blue" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#A100FF" Click="Swatch_Click" Tag="Purple" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#FF0055" Click="Swatch_Click" Tag="Crimson" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#00C853" Click="Swatch_Click" Tag="Emerald" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#00BFA5" Click="Swatch_Click" Tag="Teal" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#FFB300" Click="Swatch_Click" Tag="Amber" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#EC407A" Click="Swatch_Click" Tag="Rose" Margin="0,0,4,0"/>
                        <Button Style="{StaticResource ModeButton}" Width="24" Height="24" Background="#5E35B1" Click="Swatch_Click" Tag="Indigo"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="5,0,5,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="FastbootBtn" Content="FASTBOOT ENGINE" Style="{StaticResource ModeButton}" Width="160" Click="FastbootMode_Click"/>
                        <Button x:Name="OdinBtn" Content="SAMSUNG ODIN" Style="{StaticResource ModeButton}" Width="160" Click="OdinMode_Click"/>
                        <Button x:Name="SideloadBtn" Content="ADB SIDELOAD" Style="{StaticResource ModeButton}" Width="160" Click="SideloadMode_Click"/>
                        <Button x:Name="ToolsBtn" Content="SYSTEM TOOLS" Style="{StaticResource ModeButton}" Width="160" Click="ToolsMode_Click"/>
                        <Button x:Name="BackupBtn" Content="BACKUP &amp; RESTORE" Style="{StaticResource ModeButton}" Width="180" Click="BackupMode_Click"/>
                    </StackPanel>

                    <Border Grid.Column="1" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="15,5"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel VerticalAlignment="Center" Margin="0,0,15,0">
                                <TextBlock Text="DEVICE CONNECTIVITY" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" FontWeight="Bold"/>
                                <TextBlock x:Name="DeviceStatusText" Text="DISCONNECTED" Foreground="{DynamicResource DangerBrush}" FontSize="12" FontWeight="Black"/>
                            </StackPanel>
                            <Button Content="SCAN DEVICE" Style="{StaticResource FlashBtn}" Width="110" Height="32" Click="DetectDevice_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="5,0,5,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <Border Style="{StaticResource GlassPanel}" Background="{DynamicResource PanelBrush}">
                        <Grid>
                            <Grid x:Name="PanelFastboot" Visibility="Visible">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="FLASH PARTITION MANAGER" FontSize="18" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,15" FontWeight="Bold"/>
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="RowsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="90"/>
                                                        <ColumnDefinition Width="80"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Label}" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontSize="12"/>
                                                    <TextBox Grid.Column="1" Style="{StaticResource PathBox}" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"/>
                                                    <Button Grid.Column="2" Content="BROWSE" Style="{StaticResource ModeButton}" Margin="5,0" Click="Browse_Click" Tag="{Binding Key}"/>
                                                    <Button Grid.Column="3" Content="FLASH" Style="{StaticResource FlashBtn}" Click="FlashOne_Click" Tag="{Binding Key}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>

                            <Grid x:Name="PanelOdin" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="SAMSUNG ODIN FLASHER" FontSize="18" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,15" FontWeight="Bold"/>
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="OdinRowsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="110"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="90"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Label}" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}" FontSize="12"/>
                                                    <TextBox Grid.Column="1" Style="{StaticResource PathBox}" Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}"/>
                                                    <Button Grid.Column="2" Content="BROWSE" Style="{StaticResource ModeButton}" Margin="5,0" Click="Browse_Click" Tag="{Binding Key}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>

                            <Grid x:Name="PanelSideload" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="ADB SIDELOAD" FontSize="18" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,15" FontWeight="Bold"/>
                                <StackPanel Grid.Row="1" VerticalAlignment="Top">
                                    <TextBlock Text="Select ZIP file to sideload:" Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="SideloadPathBox" Style="{StaticResource PathBox}"/>
                                        <Button Grid.Column="1" Content="BROWSE" Style="{StaticResource ModeButton}" Margin="5,0,0,0" Click="Browse_Click" Tag="sideload"/>
                                    </Grid>
                                    <Button Content="START SIDELOAD" Style="{StaticResource FlashBtn}" Height="40" Margin="0,15,0,0" Click="FlashAll_Click"/>
                                </StackPanel>
                            </Grid>

                            <Grid x:Name="PanelTools" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="SYSTEM TOOLS" FontSize="18" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,15" FontWeight="Bold"/>
                                <StackPanel Grid.Row="1">
                                    <Button Content="INSTALL ADB DRIVERS (ZADIG)" Style="{StaticResource ModeButton}" Height="40" Margin="0,0,0,8" Width="Auto" Click="QuickCmd_Click" Tag="zadig"/>
                                    <Button Content="REBOOT TO RECOVERY" Style="{StaticResource ModeButton}" Height="40" Margin="0,0,0,8" Width="Auto" Click="QuickCmd_Click" Tag="adb reboot recovery"/>
                                    <Button Content="REBOOT TO DOWNLOAD MODE" Style="{StaticResource ModeButton}" Height="40" Margin="0,0,0,8" Width="Auto" Click="QuickCmd_Click" Tag="adb reboot download"/>
                                    <Button Content="WIPE CACHE PARTITION" Style="{StaticResource ModeButton}" Height="40" Width="Auto" Foreground="{DynamicResource DangerBrush}" Click="QuickCmd_Click" Tag="fastboot erase cache"/>
                                </StackPanel>
                            </Grid>

                            <Grid x:Name="PanelBackup" Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="BACKUP &amp; RESTORE" FontSize="18" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,12" FontWeight="Bold"/>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                    <Button x:Name="BackupSubBtn" Content="BACKUP" Tag="backup" Width="120" Style="{StaticResource ModeButton}" Click="BackupSubTab_Click"/>
                                    <Button x:Name="RestoreSubBtn" Content="RESTORE" Tag="restore" Width="120" Style="{StaticResource ModeButton}" Click="BackupSubTab_Click"/>
                                </StackPanel>

                                <Grid x:Name="BackupSubPanel" Grid.Row="2" Visibility="Visible">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <CheckBox x:Name="ChkBackupApk" Content="APK" IsChecked="True" Margin="0,0,15,0"/>
                                        <CheckBox x:Name="ChkBackupData" Content="Data" IsChecked="True" Margin="0,0,15,0"/>
                                        <CheckBox x:Name="ChkBackupUserDe" Content="User_DE" Margin="0,0,15,0"/>
                                        <CheckBox x:Name="ChkBackupObb" Content="OBB"/>
                                    </StackPanel>

                                    <Grid Grid.Row="1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <TextBlock x:Name="AppListHeader" Text="INSTALLED APPS" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                            <Button Content="Select All" Width="90" Style="{StaticResource ModeButton}" Click="SelectAllApps_Click"/>
                                            <Button Content="Clear" Width="70" Style="{StaticResource ModeButton}" Click="ClearApps_Click"/>
                                            <Button Content="Refresh" Width="90" Style="{StaticResource ModeButton}" Click="RefreshApps_Click"/>
                                        </StackPanel>

                                        <ListBox x:Name="AppListBox" Grid.Row="1" SelectionMode="Extended"
                                                 Background="{DynamicResource BgInputBrush}"
                                                 BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                                    </Grid>

                                    <ProgressBar x:Name="BackupProgress" Grid.Row="2" Height="10" Maximum="100" Value="0" Margin="0,10,0,4"/>

                                    <Grid Grid.Row="3" Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="120"/>
                                        </Grid.ColumnDefinitions>
                                        <Button x:Name="StartBackupBtn" Grid.Column="0" Content="START BACKUP" Style="{StaticResource FlashBtn}" Height="40" Margin="0,0,10,0" Click="StartBackup_Click"/>
                                        <Button x:Name="StopBackupBtn" Grid.Column="1" Content="STOP" Style="{StaticResource ModeButton}" Height="40" Click="StopBackup_Click"/>
                                    </Grid>
                                </Grid>

                                <Grid x:Name="RestoreSubPanel" Grid.Row="2" Visibility="Collapsed">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <DataGrid x:Name="BackupListGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="App" Binding="{Binding DisplayName}" Width="*"/>
                                            <DataGridTextColumn Header="Package" Binding="{Binding PackageName}" Width="*"/>
                                            <DataGridTextColumn Header="Date" Binding="{Binding BackupDate}" Width="170"/>
                                            <DataGridTemplateColumn Header="Action" Width="120">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="RESTORE" Tag="{Binding BackupPath}" Style="{StaticResource FlashBtn}" Height="28" Click="RestoreEntry_Click"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>

                                    <TextBlock x:Name="BackupRootText" Grid.Row="1" Margin="0,8,0,0" Foreground="{DynamicResource TextMutedBrush}"/>
                                </Grid>

                                <TextBlock x:Name="BackupProgressText" Grid.Row="3" Margin="0,8,0,0" Foreground="{DynamicResource AccentBrush}" Visibility="Collapsed"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Button x:Name="TabCmdBtn" Content="COMMANDS" Style="{StaticResource ModeButton}" Width="140" Click="TabCmd_Click"/>
                            <Button x:Name="TabOptBtn" Content="OPTIONS" Style="{StaticResource ModeButton}" Width="140" Click="TabOptions_Click"/>
                        </StackPanel>

                        <StackPanel x:Name="TabCmdPanel">
                            <Border Style="{StaticResource GlassPanel}" Height="200" Margin="0,0,0,12">
                                <Grid>
                                    <TextBlock Text="COMMAND PREVIEW" FontSize="12" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,0,5" VerticalAlignment="Top"/>
                                    <TextBox x:Name="CommandPreviewBox" Margin="0,25,0,0" IsReadOnly="True"
                                             Background="{DynamicResource CmdPreviewBgBrush}"
                                             BorderThickness="0"
                                             Foreground="{DynamicResource CmdPreviewFgBrush}"
                                             FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                                </Grid>
                            </Border>

                            <Border Style="{StaticResource GlassPanel}">
                                <StackPanel>
                                    <TextBlock Text="QUICK ACTIONS" FontSize="12" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                                    <Button Content="REBOOT TO SYSTEM" Style="{StaticResource ModeButton}" Width="Auto" Margin="0,0,0,8" Tag="adb reboot" Click="QuickCmd_Click"/>
                                    <Button Content="REBOOT BOOTLOADER" Style="{StaticResource ModeButton}" Width="Auto" Margin="0,0,0,8" Tag="adb reboot bootloader" Click="QuickCmd_Click"/>
                                    <Button Content="ERASE USERDATA" Style="{StaticResource ModeButton}" Width="Auto" Foreground="{DynamicResource DangerBrush}" Tag="fastboot erase userdata" Click="QuickCmd_Click"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel x:Name="TabOptionsPanel" Visibility="Collapsed">
                            <Border Style="{StaticResource GlassPanel}">
                                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="470">
                                    <StackPanel>
                                        <TextBlock Text="FLASH OPTIONS" FontSize="12" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                                        <CheckBox Content="Skip verification" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,8"/>
                                        <CheckBox Content="Wipe userdata after flash" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,8"/>
                                        <CheckBox Content="Reboot after flash" Foreground="{DynamicResource TextMutedBrush}" IsChecked="True"/>

                                        <Separator Margin="0,10,0,10"/>

                                        <TextBlock Text="UI STYLE" FontSize="12" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
                                        <CheckBox x:Name="MaterialModeCheck" Content="Enable Material Design Mode"
                                                  Foreground="{DynamicResource ForegroundBrush}"
                                                  Click="MaterialModeToggle_Click"
                                                  Margin="0,0,0,10"/>

                                        <TextBlock Text="Primary (#RRGGBB)" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                                        <TextBox x:Name="MaterialPrimaryBox" Style="{StaticResource PathBox}" Margin="0,3,0,8"/>

                                        <TextBlock Text="Surface (#RRGGBB)" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                                        <TextBox x:Name="MaterialSurfaceBox" Style="{StaticResource PathBox}" Margin="0,3,0,8"/>

                                        <TextBlock Text="Background (#RRGGBB)" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                                        <TextBox x:Name="MaterialBackgroundBox" Style="{StaticResource PathBox}" Margin="0,3,0,8"/>

                                        <TextBlock Text="Text (#RRGGBB)" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                                        <TextBox x:Name="MaterialTextBox" Style="{StaticResource PathBox}" Margin="0,3,0,10"/>

                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="APPLY MATERIAL COLORS" Style="{StaticResource FlashBtn}" Width="180" Click="ApplyMaterialColors_Click"/>
                                            <Button Content="RESET" Style="{StaticResource ModeButton}" Width="90" Margin="8,0,0,0" Click="ResetMaterialColors_Click"/>
                                        </StackPanel>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="3" Margin="5,15,5,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock x:Name="ProgressStatusText" Text="Ready to Process..." Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,0,0,5"/>
                        <ProgressBar x:Name="MainProgress" Height="12" Background="{DynamicResource CardBrush}" Foreground="{DynamicResource AccentBrush}" BorderThickness="0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Bottom">
                        <Button x:Name="FlashAllBtn" Content="START FLASHING" Style="{StaticResource FlashBtn}" Width="180" Height="40" FontSize="14" Click="FlashAll_Click"/>
                        <Button x:Name="CancelBtn" Content="STOP" Style="{StaticResource ModeButton}" Width="90" Height="40" Margin="10,0,0,0" Foreground="{DynamicResource DangerBrush}" Click="Cancel_Click"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="4" Margin="5,0,5,5" Background="{DynamicResource LogBgBrush}" CornerRadius="10"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                    <Grid>
                        <TextBlock Text="TURBO TERMINAL OUTPUT" Foreground="{DynamicResource BorderBrush}" FontSize="10" FontWeight="Bold" Margin="10,5" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                        <TextBox x:Name="LogBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                                 Background="Transparent" Foreground="{DynamicResource ForegroundBrush}" BorderThickness="0"
                                 FontFamily="Consolas" FontSize="12" Padding="12" AcceptsReturn="True"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
